# 미모러닝(MIMORunning) 기능설명서

> "걷고 뛰기만 하세요. 정리는 미모러닝이 합니다."
> 최종 업데이트: 2026-09-18

---

## 목차
1. [앱 개요](#1-앱-개요)
2. [앱 구조 — 탭 3개](#2-앱-구조--탭-3개)
3. [화면별 기능](#3-화면별-기능)
4. [공유 카드 시스템](#4-공유-카드-시스템)
5. [멀티 클립·슬라이드 편집](#5-멀티-클립슬라이드-편집)
6. [데이터 엔진](#6-데이터-엔진)
7. [건강·분석 서비스](#7-건강분석-서비스)
8. [데이터 모델 요약](#8-데이터-모델-요약)
9. [기술 스택 현황](#9-기술-스택-현황)

---

## 1. 앱 개요

| 항목 | 내용 |
|---|---|
| **앱 이름** | 미모러닝 / MIMORunning |
| **대상 플랫폼** | iOS 17+ |
| **데이터 소스** | HealthKit (읽기 전용) |
| **핵심 목적** | 러닝 기록 자동 정리 → 발전 확인 → 쉬운 공유 |
| **브랜드 컬러** | 바이올렛 #7C5CFC |
| **색 규칙: 노랑 = 실제로 한 것** | 노랑은 사용자가 실제로 해낸 값에만 쓴다 — 백테스트의 실제 기록·이미 뛴 대회명, 주차 표의 실제 롱런·주간 거리. 예측·계획 값(보라, 흰색 0.55~0.70)과 앞으로 나갈 대회 목록(흰색)에는 쓰지 않는다. 두 자리 이상으로 늘리면 강조가 사라진다 |
| **지원 언어** | 한국어 / 영어 (AppLanguage 토글) |

---

## 2. 앱 구조 — 탭 3개

```
앱
├── 기록(Activities)   — 런/걷기 리스트 + 상세
├── 성장(Growth)       — 추이·히트맵·PR·마일스톤
└── 나(Me)             — 통계·신발·뱃지·설정
```

### 온보딩
- HealthKit 권한 요청 화면(ConnectView): MIMO 브랜드 그라디언트 + "Just walk and run" 메시지
- 권한 미허용 시 기록 탭에 안내 상태 표시

---

## 3. 화면별 기능

### 3.1 기록 탭 (`ActivityListView`)

| 항목 | 세부 내용 |
|---|---|
| **이번 주 요약** | 이번 주 누적 거리·회수·시간 |
| **활동 목록** | 러닝·걷기·하이킹 카드 (있는 지표만 표시) |
| **쉬는 날 항목** | OneLinerEntry — 기록 없는 날 한마디·사진 |
| **탭 → 상세** | ActivityDetailView로 네비게이션 |
| **당겨 새로고침** | HealthKit 증분 동기화 |
| **상태 처리** | 로딩 / 빈 목록 / 권한 거부 / 에러 |

### 3.2 활동 상세 (`ActivityDetailView`)

#### 데이터 패널 종류
| 패널 | 표시 내용 |
|---|---|
| combined | 페이스·심박·케이던스 복합 차트 |
| map | 야외 GPS 경로 지도 |
| splits | km 별 스플릿 표 |
| heartRate | 심박 구간 분포 + 운동 후 심박수(회복) 3분 차트 — 종료심박 ≥ 최대심박 80%·60초 샘플 있을 때만 |
| cadence | 케이던스 시계열 |
| groundContact | 지면접촉시간(GCT) |
| strideLength | 보폭 |
| power | 파워 |
| verticalOscillation | 수직 진동 |
| elevation | 고도 프로파일 |
| intervals | 인터벌 구간 분석 |

#### 인사이트 탭 카드 — 퍼포먼스 (`PerformanceInsightCard`)
- **강도 분포 · 4주**(왼쪽 50%): 저강도/중간/고강도 **가로 막대 3행**(위→아래 순서, 행 간격 6pt, 제목↔첫 행 14pt(제목 블록과 막대를 뚜렷히 분리), 마지막 행↔점선 설명 8pt) — 각 행은 `[유형 라벨 8.5pt·폭 30] [트랙(높이 10, 흰색 6% 바탕에 강도 색 85% 채움) + 지구력 종목 **문헌값** 밴드(흰색 28%, **막대 위에 얹음** — 중간 0~5% 띠가 막대에 가려지지 않게)·**세로 점선 눈금**(80% / 0~5% / 15~20%)] [값 8.5pt·폭 26 우측정렬 "25%" — 분은 제목의 총 분만]`. 트랙 폭은 **GeometryReader 하나**로 재서 세 행이 함께 쓴다(축 = 0…max(관측 최대, 문헌값) × 1.06). 설명 "점선 - 문헌값 (지구력 종목)"은 **막대 아래** 한 줄, 오른쪽 정렬. 카드 맨 아래의 긴 각주(문헌값 근거·AT1/AT2 설명)는 **강도 분포가 실제로 그려졌을 때만** 붙는다(`PerformanceInsightCard`가 `hasIntensityDist` 바인딩으로 부모에 알림 — 심박 존 데이터가 없어 차트가 빠지면 각주도 숨김)
- **강도 부하 · 14일**(오른쪽 50%): 성장 탭 "러닝 흐름"과 **같은 `RecordBarChart`**(§5.8 — 내보내기 + **compact 모드**: 축 머리·각주 없음, 작은 범례(6pt, **날짜 축 아래·오른쪽 정렬**), 8pt 축 라벨, 플롯 높이 104·패딩 4·좌측 여백 24, x축 라벨은 ≤8 버킷 이틀·9~16 버킷 **사흘** 간격, 막대 폭 비율 9~16 버킷에서 **0.56**(성장 탭 12주는 0.6) — 별도 레이아웃 없이 파라미터로만)를 **이 러닝 날짜로 끝나는 14일**(일 버킷)로 표시. 14일 모두 같은 진하기(이전 7일 비교는 캡션 숫자로), 그 위에 **이 러닝 날 강조**(세로 점선, 말풍선·탭 선택 없음). 숫자·문장은 그대로 **7일 기준**. 거리 막대 색 = 강도, 페이스 선(위가 빠름), **왼쪽 축 페이스 · 오른쪽 축 km**는 성장 탭 그대로. 제목 "강도 부하 · 14일 (이 러닝 N AU)", 차트 아래 한 줄 "7일 N AU · **이전 7일 N AU**(0이면 생략)" + 한 줄 **"4주 평균 대비 낮음/유지/높음/크게 높음, 짧은 방향 문구"**(회복 방향으로 진행 중 / 부하 조금 높은 편 / 부하 크게 증가 중 — 회복 챙기기 / 부하 편차 적음 — 강약 나누기; 둘 중 하나만 있으면 그것만, 줄바꿈 없이 축소) + 마지막 한 줄 **"AU = 체감 강도 × 분 · 7일 합을 4주 평균과 비교해요"**(캡션과 같은 8pt 45%)(성장 탭과 같은 표시 방식 — 러닝 유형을 보여주지 않는 카드라 유형별 평소 강도 눈금은 그리지 않음)
- **페이스 분포**(유산소 피트니스 게이지 오른쪽 압축 차트): 스플릿 막대 위에 **막대 최고 높이를 3등분하는 가로 눈금 3개**(1/3·2/3·위)와 그 높이에 해당하는 **페이스 라벨(왼쪽 26pt 여백, 6.5pt)**. 평균 점선·오른쪽 평균 페이스 라벨은 그대로
- 두 블록은 **한 행 50 : 50**(왼쪽 강도 분포 · 0.5pt 세로 구분선 · 오른쪽 강도 부하). 부하가 없으면 구분선·오른쪽 열 없이 **강도 분포만 전체 폭**(트랙이 그만큼 길어진다). 강도 분포 아래 두 문장("훈련 시간의 N%가 저강도" · "이지런으로 계획한 러닝 N회")은 그대로

#### 비동기 로딩 흐름
1. HealthKit에서 Activity Detail 패치
2. HR 타임시리즈 로드(연결 샘플 → 시리즈 폴백)
3. HR 존 계산(비동기)
4. 세그먼트 기반 차트 데이터 빌드
5. RunInsightEngine 실행(인터벌/템포/이지런/장거리 분석)
6. 대회 감지 및 힐 스팟 감지

#### 운동 강도(RPE) 입력 (`EffortScaleView`, 러닝만)
- 러닝화 피커 아래 10막대(파랑→빨강, `EffortPalette` = 심박존 5색 보간) · 4구간 라벨(쉬움 1~3 · 보통 4~6 · 힘듦 7~8 · 전력 9~10)
- 우선순위: 내 입력 > Apple 수동(`workoutEffortScore`) > Apple 추정(`estimatedWorkoutEffortScore`). 출처 배지 "내 입력 / Apple 입력 / Apple 추정"
- Apple 값만 있으면 읽기 전용 → 배지 탭으로 편집. 내 입력이 있으면 "Apple 값으로" 되돌리기. Apple 값은 편집·덮어쓴 뒤에도 흰 테두리로 표식
- 탭·가로 드래그(세로 스크롤 우세 시 무시)로 입력, 손 뗄 때 `WorkoutStory.effortRPE`/`effortUpdatedAt` 저장. 강도만 있는 스토리는 일기 칩을 만들지 않음
- 상세 진입 시 Apple 값 재조회(비차단) → 값이 바뀌면 인사이트 재계산
- 헤더 아래 **같은 유형 평소 강도 한 줄** — "이지런 평소 3 · 8주 12회 · 내 입력 기준"(수동 3건 미만이면 "Apple 값 포함"). 같은 유형이 8주에 3건 미만이면 12주로 넓혀 세고, 그래도 3건 미만이면 "이지런 평소 강도 — 아직 2회(3회부터 계산)". 유형 미분류·표본 0이면 줄 자체를 생략

#### 공유 액션 (상세 화면 내)
- **Detail Panel Share**: 지표 1개짜리 4:5 카드 내보내기
- **Chart Replay Export**: 차트 시계열 영상 내보내기
- **Multi-Clip Editor**: 멀티 클립 영상 편집 → 내보내기
- **전체 카드 시트 열기**: ShareCardView (카드 7종 선택)

### 3.3 성장 탭 (`GrowthView`)

| 항목 | 세부 내용 |
|---|---|
| **일별 히트맵** | 월 달력뷰; km 기준 색 농도; 미래일 회색 |
| **러닝 흐름 카드** | 제목은 기간과 무관하게 **"러닝 흐름"**(영문 "Running Flow"). 기간 토글은 **일 · 주 두 개뿐**(월 제거). 거리와 페이스를 **한 차트에**, 각자 **실제 축**으로 그린다(`RecordBarChart` + `RecordSeries`). **정규화 없음 · 칩 없음 · 부하 선 없음 · 심박 줄 없음**(부하는 막대 길이 × 색이, 심박은 페이스가 대체로 말해 주며 정확한 bpm은 말풍선에). ① **거리** — 막대, **오른쪽(trailing) 축 = km**(눈금 3개), **막대 색 = 그 구간 평균 강도**(1~10 팔레트, 강도 기록이 없으면 회색) ② **페이스** — 선(청록) + 점(색 = 강도, 막대 위에서도 보이도록 **어두운 테두리**), **왼쪽(leading) 축 = 실제 페이스**(`6'18"` 표기, **위 = 빠름**, 눈금 3개는 10초 단위로 반올림), 범위는 최소~최대 ±10초이되 페이스 표본이 8개 이상이면 **5~95 백분위로 클립**(극단값이 나머지를 눌러 앉히지 않게), 기간 **거리 가중 평균**(`RecordSeries.paceBaseline`)에 **점선**(값 라벨은 축 머리에만 — 플롯 안 라벨은 오른쪽 막대와 겹쳐 제거). Swift Charts는 차트당 y 스케일이 하나뿐이라 **페이스를 km 도메인 위로 매핑**해 그린다(`kmTop = max(1, 최대 km) × 1.08`, `y(pace) = kmTop × (slow − pace) / (slow − fast)`, 왼쪽 축 라벨은 이 매핑의 **역함수**로 페이스 글자를 되돌린다). 페이스가 하나도 없으면 왼쪽 축이 km인 평범한 막대 차트. 기록이 있는 구간만 점을 찍고 짧은 공백은 이어 그리되 **14일 이상 비면 선을 끊는다**(monotone). 플롯 높이 180. **탭 하나가 막대·점을 함께 강조** — 그 구간이 진해지고 나머지는 흐려지며(0.55) 점선 세로선이 서고, 카드 머리에 **말풍선 하나**가 뜬다(일간 "9/8 · 5.56km · 36:18 · 강도 3 · 73 AU · 6'31\" · 136 bpm" / 주·월간 "8/25–8/31 · 42.0 km · 4회 · 1,280 AU · 평균 6'20\" · 142 bpm" — 없는 값은 생략). 머리에 **1~10 강도 색 범례**와 축 머리("평균 6'18\" · 가장 빠른 5'56\"" 왼쪽 / "거리 km · 165 km · 22회" 오른쪽), 차트에는 축선 3개(페이스 라벨 오른쪽·km 라벨 왼쪽 세로선, 날짜 위 가로선, 0.5pt 흰색 22%), 차트 아래 **각주** "막대 = 거리(색 = 강도) · 선 = 페이스, 위가 빠름 · 왼쪽 축 페이스 · 오른쪽 축 km". **기간 이동**: 일간은 기본 오늘까지 **롤링 최근 30일**, "◀"로 그 이전은 **전월 달력 단위**(1일~말일), "▶"로 되돌아오면 다시 롤링 30일 / 주간 12주(월요일 시작). **월간은 제공하지 않는다**(`RecordPeriod.month`는 엔진에만 남아 있음). 차트 아래 **✦ 상태 한 줄**(기간 라벨 · 횟수 · km · 쉬운 날 %)과 **방향 한 줄**(§6.11 `RecordFlowInsight` — 앞뒤 절반 비교: 거리 ±10% · 페이스 ±5초 · 강도 ±1 → 우선 6개 규칙 + 나머지 조합 7개를 강도 방향 축으로 채워 **표본만 있으면 방향 문장이 항상 하나**, 절반에 3회 미만이면 상태 줄만). 공유 카드에도 같은 두 줄을 싣고, compact(퍼포먼스 7일)에는 넣지 않는다 |
| **PR 패널** | 5K / 10K / 하프 / 풀 기록; 최근 30일 이내 "new" 뱃지 |
| **마일스톤 타임라인** | 첫 런 / 거리 PR / 누적 마일스톤 / 최장 런 |
| **추세 지표 선택기** | 케이던스·파워·GCT·보폭·수직진동·VO2·1분 회복·체성분 시계열(MetricTrendView) |
| **유형별 평소 강도** | 부하 카드 바로 아래 표(`EffortTypeBaselineCard`). 유형별 **최근 8주 강도 중앙값**(같은 유형 3건 이상일 때만, 전체 러닝 폴백 없음) — **3회 미만이면 12주로 확장**해 다시 셈. 행마다 "N · 쉬움/보통/힘듦/전력"(강도색)·"8주 12회", 수동 입력만으로 계산된 유형은 바이올렛 점 + "내 입력". 표본 0인 유형은 생략, 순서는 이지런·LSD·롱런·템포·인터벌·거리주·빌드업·대회·일반 고정 |
| **쉬운 날 페이스** | **본인 이지·LSD 강도 중앙값 이하**(최근 12개월 3건 미만이면 4 이하) 러닝의 페이스 시계열(스파크 카드) + 3개월 대 3개월 MDC 판정으로 빨라진 쪽만 문구(`EffortPaceTrend`). 템포·인터벌·대회는 제외 |
| **회복 관찰** | 종료심박·기온을 회귀로 통제한 1분 회복 잔차, 최근 3개월이 이전 3개월보다 유의하게(MDC) 좋아졌을 때만 문구. 나빠진 쪽은 침묵 |
| **피로 신호** | 주간 패턴 p5 분석 → InsightEngine 피로 플래그 업데이트 |
| **성장 카드 공유** | WeeklyGrowthShareCardView. "흐름 내보내기"(`MileageStreakShareCardScreen`, 300×560, 하단 러닝 아이콘 없음, ✦ 두 줄은 섹션 제목(13pt)보다 작은 11.5pt·.primary 색)는 **러닝 흐름 차트 + 잔디(히트맵)** 딱 두 장면. 차트는 §5.8에 따라 성장 탭과 **같은 `RecordBarChart`**를 `exportMode: true`로 재사용한다(탭 안내·선택 상호작용 제거, 플롯 높이 130, 패딩 12, 범례·축 머리·각주는 유지, `cardBackground`로 공유 테마 색 주입) — 카드 전용 막대 차트를 따로 만들지 않는다. 카드 머리에 "러닝 흐름 + 기간 라벨"(최근 30일 / 8월 / 최근 12주), 아래에 잔디·연속 주·활동일 |
| **조언 카드** | 내구성·근력·케이던스 제안, 최대 2건, 탭하여 운동·근거 펼침 |

### 3.4 나 탭 (`MeView`)

| 항목 | 세부 내용 |
|---|---|
| **기간 통계** | 월별/연도별 누적; 전월 대비 ±% |
| **신발 추적** | 이름·브랜드·누적 km |
| **뱃지** | 첫 5K / 누적 km / 연속 스트릭 등 마일스톤 |
| **월간 폼 지표** | 케이던스·GCT·보폭·VO2 추세 |
| **회복 주 강도 문장** | 훈련 계획 주차 표에서 이번 주가 회복·테이퍼 단계인데 평균 강도가 최근 8주 중앙값+1 이상이면 노란 한 줄 |
| **Garmin 알림** | Garmin 소스 감지 시 조건부 안내 |
| **거리 단위** | km ↔ 마일 토글 |
| **스토리·클라우드** | CloudKit 동기화 상태 표시 |

---

## 4. 공유 카드 시스템

### 4.1 카드 종류 3가지 (카드 만들기 캐러셀 순서 = `ShareCard.allCases`)

| # | 카드명 | 키워드 |
|---|---|---|
| 0 | **스탬프(Stamp)** | 사진 위 텍스트 스탬프 |
| 1 | **원라이너(OneLiner)** | 짧은 텍스트 강조 |
| 2 | **애슬레틱(Athletic)** | 개인 프로필·기록 |

> 스카이·ECG·티켓 카드는 2026-09-24 삭제(사용 빈도 낮음, 정지 이미지 1종만 지원). 하늘 그라디언트(`SkyPalette`)는 원라이너 배경으로 남음.
> 빅넘버 카드도 같은 날 삭제(스탬프 "거리 몰아주기"·애슬레틱과 역할 중복).
> 플레이서블 카드도 같은 날 삭제(스탬프 행마다 라벨·가로 3열·루트 사이드 + 9칸 위치·문구와 역할 중복).

추가 특수 카드:
- **요약 카드(Summary)** — 핵심 지표 + 스플릿 바 + 날씨
- **스플릿 카드(Splits)** — km 별 상세 표
- **성장 카드(Growth)** — 기간 비교·마일스톤
- **포토 카드(Photo)** — 사진 오버레이
- **비디오 오버레이 카드(VideoOverlay)** — 영상 위 지표 크롤

### 4.2 카드별 출력 형태(템플릿, `ShareTemplate`)

탭 이름 = 배경에 무엇을 쓰는지. 카드별 지원 범위는 `ShareCard.supportedTemplates`.

| 템플릿 | 비율 | 용도 | 지원 카드 |
|---|---|---|---|
| **기록** (`.record`) | 4:5 | 사진 없는 기록 전용 정지 카드 (§5.7 기록 전용 모드) | 애슬레틱 |
| **사진** (`.photo`) | 4:5 | 사진 배경 정지 카드 | 전체 |
| **영상** (`.video`) | 9:16 | 영상 클립 위 텍스트/지표 | 전체 |
| **슬라이드** (`.slide`) | 9:16 | 여러 장 사진 슬라이드 | 전체 |
| **경로 영상** (`.routeVideo`) | 9:16 | 지도 위 경로가 그려지는 영상 | 스탬프·애슬레틱 |

> 2026-09-24: "애슬레틱" 템플릿 → "기록", "스토리" 템플릿 → "사진"으로 개명(카드 이름·인스타 스토리 비율과 혼동 방지).

### 4.4 원라이너 카드 (`OneLinerCard`)

- 텍스트 3줄 (각 30자 이하)
- 영상 클립 슬롯 텍스트(각 24자)
- 3×3 위치 그리드
- 폰트 / 색상 칩 선택
- 애니메이션: 타이핑·페이드·플라이인 효과
- SwiftData 멀티클립 레시피 저장

### 4.5 스탬프 카드 (`StampCard`)

#### 스탬프 템플릿 9종
`hud` / `receipt` / `scoreboard` / `passportStamp` / `circleBadge` / `labeledRows` / `verticalLabel` / `distanceHero` / `mixedAlign`

#### 스탬프 색상 모드
`auto` / `brand(바이올렛)` / `ink(흑백)` / `lime` / `red`

#### 위치 모드
`free9`(9점 자유) / `band3`(3단 밴드) / `fixed`(고정)

#### 애니메이션
스탬프 바운스 / 페이드 / 플라이인 (슬라이드 내보내기 시 적용)

#### 출력 지원
- 단일 사진 스토리(StampStoryTemplate)
- 다중 사진 Ken Burns 슬라이드(StampSlideTemplate)
- 영상 위 스탬프 오버레이(StampVideoTemplate)

### 4.6 공유 카드 통일성 규칙

| 요소 | 기준값(scale=1) |
|---|---|
| 워드마크 | 9pt |
| 인사이트 제목 | 13pt bold |
| 차트 영역 | 160×100pt |
| MiniMe | 54pt |
| 아이콘/레이블 | 7pt / 8pt |
| 수평 패딩 | 10pt |

모든 영상·슬라이드 오버레이는 `VideoOverlayCard(scale:)` 단일 컴포넌트 사용. 별도 레이아웃 복사 금지.

---

## 5. 멀티 클립·슬라이드 편집

### 5.1 멀티 클립 에디터 (`MultiClipEditorView`)

| 기능 | 세부 내용 |
|---|---|
| **클립 추가** | 사진 앱에서 비디오/사진 선택(PHPicker) |
| **클립 스트립** | 수평 스크롤, 재정렬 지원 |
| **트림 편집** | ClipTrimView — 드래그 핸들로 시작·끝 조절 |
| **지표 칩 토글** | 거리·페이스·심박·케이던스 등 오버레이 항목 선택 |
| **실시간 프리뷰** | 4:5 컨테이너 안 9:16 프리뷰(scale ≈ 0.703) |
| **내보내기** | 1080×1920 영상 → 카메라 롤 저장 |

### 5.2 영상 합성 (`MultiClipComposition`)

- AVFoundation 기반 멀티 클립 합성
- PHAsset 해상도 해결 + 트림 기간 처리
- 1080×1920 HDR→SDR 톤 매핑 + 필름 컬러 그레이딩

### 5.3 사진 슬라이드 합성 (`PhotoSlideComposition`)

- Ken Burns 애니메이션(교번 스케일·팬)
- 사진 간 크로스 디졸브
- CALayer 기반 1080×1920 내보내기
- 슬라이드당 3초 기본 재생 시간

### 5.4 경로 영상 (`RouteVideoService`)

- GPS 폴리라인 그라디언트(심박 기반 색상)
- 렌더된 경로 좌표 + 지표 패널

### 5.5 차트 리플레이 영상 (`RunChartReplayExporter`)

- 구간 바 차트(페이스·심박·케이던스·보폭·고도)
- X/Y 축 + 그리드 라인 렌더링
- 시계열 애니메이션 영상

---

## 6. 데이터 엔진

### 6.1 HealthKit 매니저 (`HealthKitManager`)

| 기능 | 세부 내용 |
|---|---|
| **권한 관리** | 20+ HealthKit 타입 요청 |
| **2단계 패치** | 캐시 우선 → HealthKit 증분 동기화 |
| **배경 동기화** | 3개월 단위 히스토리 백필 |
| **타임시리즈 캐시** | HR·케이던스·파워 샘플 디스크 JSON 캐시 |
| **디테일 패치** | 경로·스플릿·존·파워·케이던스·GCT·보폭·VO2·고도 비동기 |
| **날씨 통합** | Open-Meteo API 백필; ConditionCache 저장 |
| **정지 구간 감지** | workout pause/resume 이벤트 파싱 |
| **고도 계산** | 정확도 필터 + 누적 상승고도 |
| **버전별 마이그레이션** | HR 시리즈 캐시 퍼지, 러닝 지표 쿼리 재작성, 날씨 백필 |

### 6.2 레벨 엔진 (`LevelEngine`)

5단계 비공개 버킷: **초보 → 하수 → 중수 → 고수 → 신**

| 항목 | 방법 |
|---|---|
| **5K 등가 환산** | Riegel 공식(다른 거리→5K) |
| **백분위 버킷** | 성별별 임계값(남: 18/22/28/35분) |
| **에이지그레이딩** | WMA 다항식 계수; 백분위와 블렌딩 |
| **VDOT** | Daniels-Gilbert 공식 |
| **강등 없음** | UserDefaults로 최고 도달 레벨 유지 |
| **출력** | bucket / ageGrade% / best5KEquivSec / vdot |

### 6.3 인사이트 엔진 (`InsightEngine`)

15가지 테마를 우선순위 계층 구조로 판정:

**우선순위 (높 → 낮)**
1. `safety` — 심박 급상승·폭염 경고
2. `firstAchievement` — 생애 첫 3K/5K/10K/하프/풀
3. `raceDay` — 대회 당일 러닝
4. `recordImproved` — 동거리 ±15% 내 90일 최고 페이스
5. `adverseCondition` — 악조건 극복
6. `tradeoff` — 효율 향상(같은 페이스 심박↓)
7. `rarityFact` — 희귀 사실(극단 기온·오랜만의 시간대·누적 마일스톤)
8. `milestone` — 누적 달성
9. `subThreshold` — 하위 임계 인식
10. `distanceExpanded` — 이번 달/분기 최장
11. `consistent` — 연속 스트릭·빈도 목표
12. `periodPositive` — 기간 하락 시 긍정 요소 발굴
13. `recovery` — 이지런 의도 인식
14. `default` — 기본 입문·출석

**인사이트 구조**
```
[테마]    recordImproved
[제목]    "한계를 미는 러닝"     ← 철학적 헤드라인
[부연]    "3주 연속, 이번 주 4회"  ← 데이터 근거
```

**AI 강화**: iOS 26+ / A17 Pro·M1 이상에서 FoundationModels로 한국어 문장 재작성(30자 이내)

### 6.4 러닝 분석 엔진 (`RunInsightEngine`)

워크아웃 유형별 세부 분석:

| 유형 | 분석 항목 |
|---|---|
| **인터벌** | 구간 일관성 / 페이드(마지막 2렙) / 회복(HR 낙차) / 볼륨 |
| **템포** | 페이스 안정성(SD ≤ 10초 목표) |
| **심폐 드리프트** | 전반/후반 HR 차이; 6% 이상 플래그 |
| **이지런** | 75% 최대HR 초과 감지 |
| **빌드업** | 점진 가속 검증(60%+ 쌍이 개선) |
| **장거리** | 히스토리 중앙값 대비 페이스 비교 |
| **폼 지표** | 케이던스(175–185 목표) / GCT(≤280ms) / 보폭 / 수직진동 |

**폼 카드 마무리 문장** (`RunFormCardView` → `FormNarrative`):
- 케이던스·GCT·보폭을 페이스 대역이 맞는 "평소 범위"와 비교한 판정(inRange/above/below)은 카드가 하고, 문장은 순수 함수 `FormNarrative.sentence(_:frame:)`가 만든다. 우선순위: GCT 이탈 → 케이던스 → 보폭 → 모두 평소 범위.
- 마무리 문장은 **러닝 유형에 따라 톤만 달라진다**(사실은 그대로, 부상·위험 표현 없음). `FormNarrative.frame(for:)`로 3 프레임 매핑:
  - **easy** (`.easy`) — 케이던스·보폭이 조금 작아도 자연스럽고 짧은 GCT는 좋은 신호. 예: "발걸음도 보폭도 평소보다 조금 작았어요. 편하게 뛴 날이에요." / "잰걸음이었어요. 편한 날엔 리듬을 조금 늦춰도 괜찮아요." / GCT 길면 "회복이 덜 된 날일 수 있어요."
  - **fast** (`.tempo`·`.buildUp`·`.race`·`.distanceRun`) — 케이던스·보폭↑, GCT↓가 기대 방향. 예: "보폭 1.31m로 속도를 냈어요. 빠른 날엔 발걸음을 조금 더 빨리 돌리는 쪽이 부담이 덜해요." / "속도를 냈는데 지면접촉이 262ms로 길었어요. 다리가 무거운 날이었을 수 있어요." / 모두 범위면 "평소와 같은 폼으로 4'20 페이스를 냈어요."
  - **general** (`.general`) — 방향 기대 없는 사실 서술(기존 문장 유지). 롱런·LSD는 장거리 문맥 문장, 인터벌은 판단 유보 문장을 카드가 따로 처리하므로 여기 오지 않음.
- 보폭 구절("보폭이 N m로 평소보다 컸어요/작았어요")은 보폭만 이탈하고 케이던스·GCT가 둘 다 이탈하지 않았을 때만 덧붙인다(3개 나열 방지).
- 버그 수정: 케이던스↓·보폭↓인데 "보폭으로 페이스를 만들었어요"라고 말하던 것을 "케이던스와 보폭이 평소보다 조금 작았어요."로 교체.
- **장거리 문맥 문장** (`FormNarrative.longDistanceSentence(_:)`, 폼 카드와 인사이트 탭 리듬 카드가 **같은 함수** 사용 — 두 카드가 서로 다른 문장을 내던 중복 코드 제거):
  - 모두 평소 범위 → "12km를 뛰면서 폼이 평소 범위 그대로였어요." / 범위 아래 없이 GCT만 위 → "지면접촉이 평소보다 조금 길었어요."
  - 케이던스·보폭 중 하나라도 평소 범위 아래일 때만 전반/후반 평균으로 **실제 방향**을 서술한다(GCT의 `.below`는 짧아짐=하락 아님이라 이 분기를 열지 않음). 케이던스 Δ≥2spm "올라갔어요/내려갔어요", 보폭 Δ≥0.02m "늘었어요/줄었어요", 미만은 "유지했어요". 예: "12km를 뛰면서 케이던스는 195spm으로 유지했고, 보폭은 0.88→0.95m로 늘었어요." / 둘 다 유지면 "케이던스 195spm, 보폭 0.91m를 끝까지 유지했어요." (이전엔 방향과 무관하게 "…로 줄었어요"로 고정)
  - 평소보다 1.5배 초과 또는 12km 이상이고 카드에 거리 인사이트 배지가 없으면 "평소보다 3.2km 긴 빌드업이에요." 접두.
  - 테스트 `FormNarrativeTests`(26 케이스: 프레임 19 + 장거리 7).
- **지표 상태 판정 공유** (`FormNarrative.status(rawValue:stat:metric:)` · `roundedDisplay` · `driftAdjustedGCT`): 폼 카드와 인사이트 탭 리듬 카드가 케이던스·GCT·보폭의 inRange/above/below를 **같은 함수**로 판정한다 — 값과 경계를 표시 정밀도(케이던스·GCT 정수, 보폭 0.01m)로 반올림해 비교하고, GCT는 두 카드 모두 시점 보정(열람 시점 잔차 − baseline 시점 잔차, R²≥0.2·|drift|≥2ms·±15ms 제한) 밴드를 쓴다. 이전엔 리듬 카드가 원값·미보정 밴드로 판정해 경계값 러닝에서 두 카드가 달랐다. 리듬 카드 케이던스 게이지 문구("평소 범위예요/빨랐어요/낮았어요")도 같은 판정 → 하단 "lo–hi" 라벨과 일치. 게이지 축 상한은 개인 분포(중앙값+4σ)와 **이 러닝 값·기준 범위 상단 +4를 5 단위로 올린 값** 중 큰 쪽 — 케이던스가 높은 러너(190 초과)에서 바늘이 끝에 붙지 않는다.
- **러닝 유형별 캡션** (`FormNarrative.hrSecondHalfRiseCaption` · `highIntensityZoneCaption` · `highIntensityOneLiner` · `formHeldCaption` · `belowRangeNote`): 빌드업·템포·대회는 후반 가속이, 인터벌까지 포함하면 고강도가 **계획**이므로 같은 관측을 경고·변명처럼 읽히지 않게 사실 문장만 바꾼다. 이지·일반·롱런·LSD·거리주 문장은 그대로.
  - 심박 차트 캡션(후반 평균 +8bpm↑): 빌드업만 "빌드업답게 후반에 심박이 올라갔어요", 템포·대회·인터벌은 "후반에 심박이 올랐어요" 그대로. 색은 유형과 무관하게 동일.
  - 심박존 도넛 캡션(4존+ 최다): 빌드업·템포·대회·인터벌 "계획대로 고강도 구간이 많았어요", 이지·일반은 "고강도 구간이 많았어요"(이지런에선 실제 관찰). 한 줄 요약(고강도 ≥40%)도 같은 규칙 + "다음엔 여유롭게 가도 좋아요" 유지.
  - 폼 유지 알약(전·후반 GCT +10ms 미만 / 케이던스 −2spm 미만): 빌드업·템포·대회 "후반 가속에도 폼이 버텼어요", 롱런·LSD 등은 "장거리인데 후반까지 폼이 버텼어요".
  - 폼 추이 km별 차트 주석(포인트 30%+ 범위 아래, 장거리 문맥일 때): 빌드업·템포·대회는 GCT "후반 페이스가 빨라 범위 아래에 머물러요", 그 외 지표 "후반 페이스가 빨라 범위를 벗어났어요"; 나머지 유형 "장거리라 평소 범위 아래에 머물러요". 띠·포인트 강조는 유형과 무관하게 평균 페이스 대역 하나로 그대로.
  - 심폐 효율 인사이트("비슷한 페이스 최근 N회 대비 심박이 N bpm 낮아요 — 심폐 효율이 개선되고 있어요"): **빌드업·인터벌은 생성하지 않음** (`RunInsightEngine.efficiencyComparisonApplies(to:)`). 빌드업 평균 페이스는 느린 전반에, 인터벌은 회복 구간에 가려져 같은 평균 페이스 러닝과의 심박 비교가 편향된다 — 참고 문구를 붙여도 숫자가 틀린 채 남으므로 제외가 맞다.
  - 테스트 `FormTypeCaptionTests`(17 케이스: 유형 그룹 2 · 캡션 7 · 상태 판정 4 · GCT 보정 2 · 효율 게이트 1 · 잔차 배선 1).
- 숫자 뒤 조사: `KoreanParticle.topic(after:)` — 마지막 자릿수 2·4·5·9 → "는", 그 외(0·1·3·6·7·8) → "은". 유산소 피트니스 등급 문구("48.5는 30대 여성 기준 높음")에 적용. 게이지 숫자 표기는 기존 그대로(정수). 테스트 `KoreanParticleTests`(4 케이스).

**폼 카드 추세 문단** (`mrFormObservation`, `MRFormStyle`):
- 케이던스·지면접촉(GCT)이 같은 페이스에서 함께 실증 변화(MDC 또는 4주+ 연속)했을 때 한 문단(최대 3줄)으로 묶는다. 1줄 사실("같은 페이스에서 케이던스가 3개월 새 3spm 올라가고, 지면접촉이 8ms 짧아졌어요.") → 2줄 역학 → 3줄 결론.
- **보폭 방향은 케이던스에서만 읽는다** — 같은 페이스에서 보폭 = 속도 ÷ 케이던스이므로 케이던스↑ = "조금 더 잦은 걸음", ↓ = "조금 더 큰 걸음"(|Δ| < 1 spm이면 보폭 표현 없음). 공중 시간(= 걸음 주기 − GCT)이 늘었다고 "더 큰 한 걸음"이라 말하던 이전 해석은 물리적으로 틀려 제거.
- 탄력 판정: GCT↓·공중↑ → "공중 시간은 Nms 늘었어요" + "같은 페이스를 더 가볍고 탄력 있게 만들고 있다는 뜻이에요". GCT↑·공중↓ → "지면에 머무는 시간이 늘었어요" + 중립 결론 "같은 페이스를 조금 더 오래 딛고 만들고 있어요"(부상·위험 표현 없음). 그 외 조합은 케이던스 방향으로 결론("더 잦은/큰 걸음으로").
- 반복 완화(저장 상태 없음): `runCadenceResidual`(이 러닝의 케이던스 잔차, `mrFormRunResidual(_:on:)`)을 넘기면 추세 끝점(`recentMean`) 대비 위치로 마무리가 달라진다. 배선: `ActivityDetailView.computeFormShifts()`가 케이던스 잔차에서 열람 러닝 당일 값을 꺼내(`[폼:잔차]` 로그) `RunInsightCardView` → `RunInsightTabCard`/`InsightExportSheet` → `RunFormCardView.runCadenceResidual`로 전달 — 끝점−1 이상 "이 러닝도 그 흐름 위에 있어요." / 끝점−2 미만 "이 러닝은 그 흐름보다 조금 느긋했어요." / 그 사이는 생략. 값이 없으면 8주+ 지속 추세일 때 가운데 줄을 빼고 사실+결론 2줄로 축약. 테스트 `FormTrendParagraphTests`(11 케이스).

**페이드 원인 분석** (`FadeAnalysis`):
- 원인 분류: `overpace` / `threshold` / `enduranceGap` / `mixed` / `unclear`
- 근거: 초반 과속 여부 / HR 상승 유지 여부 / 최근 장거리 km

### 6.5 워크아웃 타입 분류기 (`WorkoutTypeClassifier`)

스플릿 데이터 + 히스토리 맥락에서 자동 분류:
`interval` / `buildUp` / `distanceRun` / `lsd` / `longRun` / `easy` / `tempo` / `general`

### 6.6 차트 데이터 빌더 (`RunChartData`)

| 레이어 | 색상 | 드로우 스타일 |
|---|---|---|
| heartRate | 존 색(파랑·청록·라임·주황·핑크) | 라인 · 타일 점도 존 그라데이션 |
| pace | 청록 | 바 |
| cadence | 노랑 | 라인 |
| elevation | 초록 | 면 채우기 (고저차 30m 미만은 평평하게 — 최소 스팬) |
| power | 라벤더 | 라인 |
| strideLength | 화이트 | 라인 |
| verticalOsc | 마젠타 | 라인 |
| aerobic · calories | 회색 | 값 전용 타일(선 없음) |

- 타임시리즈 → 거리시리즈 변환(스플릿 경계 기준)
- 최대 300포인트 다운샘플링
- HR 존 밴드 오버레이
- 페이스 노멀: 느릴수록 낮게(직관적 시각화)
- 타일 기본 전부 켬, 탭으로 토글(저장 키 v2) · X축은 km별 페이스 한 줄 + "시간 · 거리" 한 줄 · 심박 선 위 장식 점 제거

### 6.7 내구성 판단 엔진 (`MRDurabilityCheck`)

- 롱런 첫 25% vs 마지막 25% 케이던스 비교 (페이스 ±5% 게이트, 초반 과속·역치 제외)
- 최근 8주 롱런 3회 중 2회 이상 ≥3% 하락 → 근력·플라이오 제안 (`durability`, B등급)
- 3개월 케이던스 하락(MDC 통과) + S1 없음 → 케이던스 큐 제안 (`cadenceCue`, B등급)
- 억제: 하프 이상 대회 D-14 · 완주 후 D+14 · 공백 복귀 21일
- 표시: 성장 탭 조언 카드 (`MRAdviceCardView`), 운동 목록·근거 펼침
- 근거: Van Hooren 2024 · Blagrove 2018 · Heiderscheit 2011

### 6.8 운동 후 심박 회복 (`MRRecovery`)

- HRR1 = 종료 30초 평균 심박 − 종료 60초 후 심박(±15초 중앙값). `heartRate` 샘플 권한으로 종료 후 180초를 직접 읽음(새 권한 없음, 워치 필요)
- 자격: 종료심박 ≥ 추정 최대심박(208−0.7×나이)의 80%, 미상이면 130bpm
- 히스토리: 12개월 러닝마다 소형 쿼리 2개 → `mimo_hrr_history_v2.json` 캐시, 새 러닝 시 재계산
- 추세: HRR1 ~ 종료심박 + 기온 회귀 잔차 → `mrFormShift`(MDC) 판정. 개선일 때만 문구, 컨디션·과훈련 판정에는 쓰지 않음
- 근거: Daanen 2012 체계적 문헌고찰. 임상 절단점(Cole 1999)은 인용하지 않음

### 6.9 훈련 계획 플래너 (`MRRacePlanner`)

- **필요 기간** = 롱런·주간 거리가 목표에 닿는 주(시뮬레이션) + 테이퍼(10K 이하 1주·하프/풀 2주) + 추가분(하프 튠업 2주 · 자기 계획 있는 단거리까지의 주 수). 3주 하한. 남으면 시작을 뒤로 미루고 앞은 "유지". **이미 시작한 계획(앵커 있음)은 시작을 미루지 않고 대회 당일까지 유지** — 3주 미만 규칙은 새 계획에만
- **롱런 목표** 5K 12 · 10K 16 · 하프 21 · 풀 28km (하프·풀은 Fokkema 2020 절단점)
- **주간 거리 목표** 5K max(현재,25) · 10K max(현재,32) · 하프 max(현재×1.2,40) · 풀 12개월 최대. 상한은 항상 12개월 최대
- **대회 페이스 단계**: 하프 이상 빌드 후반 25% — "롱런 마지막 15분(60분 미만 10분)은 예측 페이스로" (관행, 통제 연구 없음)
- **앞선 대회 회복 블록**: 풀 3주(30/50/70%) · 하프 1주(60/70%) · 10K 이하 없음. 계획 카드 상단에 "앞 계획 따름 → 대회 주 → 이 계획 시작" 타임라인
- **튠업 대회**: A 레이스(하프 이상)만 계획. 그 안의 5K·10K(풀 안의 하프)는 "대회 주"로 흡수 — 단거리는 롱런 유지(진행 없이 직전 정점)·주간 −20%, 하프는 대회가 롱런·주간 −30%·다음 주 회복. 대회 주 3주 연속이면 고지
- **자기 계획이 있는 단거리**(스냅샷 앵커 있음): 흡수하지 않고 독립 카드 유지. A 계획은 **겹치는 주에 그 계획의 롱런·주간·롱런 분·실행 안내를 그대로 따르고**(단계 = "10K 계획", 안내 = "10K 계획을 따릅니다 · …", 주차표 범례에 한 줄), 그 전 주는 테이퍼로 양보, A 고유 진행은 대회 이후 재개. 따르는 숫자는 **그 단거리의 스냅샷(확정 주차)** 이다 — 라이브 재계산 값은 오늘 프로필로 다시 만들어져 주차표와 어긋날 수 있으므로(`MREngineStore.recomputePlans(snapshotAnchors:snapshotWeeks:)` → `MRPlanGovernance.applyingSnapshot`). 두 카드가 같은 주에 같은 숫자·같은 이행 기호를 보여준다. **대회 뒤 재개는 직전에 실제로 뛰게 한 롱런 +10%부터** — 따르는 주가 롱런 정점을 그 주 값으로 맞추되 테이퍼·회복 주는 정점을 깎지 않는다(10.4 다음 주에 21로 뛰지 않음). 조합마다 표는 달라도 원칙은 이 하나
- **이번 주를 다스리는 계획**(`MRPlanGovernance.governingWeek`): 계획이 둘 이상 겹치는 주에 "이번 주 목표"를 묻는 곳은 전부 이 하나의 규칙을 쓴다 — 그 월요일에 주차가 있는 계획 가운데 **대회일이 가장 이른 계획**이 그 대회 주까지 다스리고, 대회가 지나면 다음 계획이 이어받는다. 스냅샷이 있으면 주차표와 같은 숫자(`MREngineStore.governingPlanWeek(for:)`). 기록 상세의 주간 목표 필·총평 훈련부하 줄의 계획 단계가 이 값을 읽는다
- **주간 목표 필**(기록 상세, `RunInsightEngine` 부하 문구): 화·수(목·금 폴백) "이번 주 12.6 / 48km · 5일 남았어요." · 토 "내일이 이번 주 마지막 날이에요 · 12.6 / 48km." · 일 "이번 주 40.2km · 계획 48km." 계획이 **둘 이상**이면 다스리는 계획의 라벨을 붙인다 — "이번 주 12.6 / 48km · **10K 계획** · 5일 남았어요."(`RunBaseline.planLabel`, 한 계획뿐이면 nil → 문구 그대로). DEBUG 로그 `[주간목표]`에 그 주의 계획별 주간 km와 선택 결과
- **판정 문턱**: 롱런 정점이 목표의 하프 0.90(19km) · 10K 이하 0.95 이상이면 "가능". 하프 0.90은 Fokkema 2020 절단점 폭에 맞춘 값
- **대회 페이스 조기 표시**: 기록 탭 D-day 카드(가장 가까운 대회 하나. 기본 접힘 = 헤드라인 + 페이스 한 줄, 탭하면 펼침·하루 단위 기억, 전날·당일·회복은 항상 펼침) — D-14~D-8 "대회 예상 평균 N/km" 한 줄 · D-7~D-2 페이스 + 첫 5km 상한(−2%) + 스플릿 표 · 전날·당일은 기존대로. 나 탭 계획 카드 헤더에 예상 페이스 상시. 기준은 목표가 아니라 예상 기록
- **주차 표 범위**: 대회일이 속한 주까지 포함 (월요일 간격으로 셈 — 일요일 대회의 마지막 주가 잘리지 않게). 테이퍼 문구는 "롱런 10km + 짧게 2.7km × 4회 · 강도는 그대로"처럼 대략 거리 표기
- **대회 삭제·복원 (양방향 무손실)**: 삭제 전 확인 알림. 스냅샷은 지우지 않고 분리(detached) 표시만 — 재등록(날짜 ±1일·거리 2% 퍼지 매치, 이름·날짜는 새 입력으로 갱신)하면 앵커·주차 표·이행 기호가 그대로 복원. 분리된 스냅샷은 아카이브(유령 "기록 없음")를 만들지 않는다. 다른 계획이 그 대회를 언급하던 주("10K 계획을 따릅니다"·"대회 주")는 **표시 시점에만** 라이브 값으로 대체하고 스냅샷 원문은 보존 → 복원하면 원래 문구가 돌아온다. 디버그 뷰에 분리된 스냅샷 목록과 복원 버튼(안전망). 한계: 같은 거리 대회가 둘일 때 하나만 지우면 라벨 매치라 언급이 남는다
- **스냅샷 규칙**: 지난 주차는 고정 · 시작 전 계획은 구조 변경 시 전체 갱신 · 시작한 계획은 튠업 관련 주(이번 주 포함)·테이퍼 여부가 바뀐 주를 갱신하고, **튠업이 있는 계획은 튠업 뒤 미래 주(재개 진행)도 라이브로 갱신**(튠업 없는 계획의 미래 주는 프로필 변화로 흔들리지 않게 고정) · 실시간 계획에만 있는 미래 주는 뒤에 덧붙임 · 예외로 다른 계획을 "따르는" 주는 지난 주도 그 계획의 고정값으로 갱신(실제 따른 계획 기준으로 이행 판정) · 앵커가 이번 주 이전일 때만 "시작한 계획"

### 6.10 운동 강도(RPE) 엔진 (`EffortResolver` · `EffortBaseline` · `EffortRules` · `EffortLoad` · `EffortPaceTrend`)

- **읽기**: iOS 18+ `HKWorkoutEffortRelationshipQuery`(`.mostRelevant`)로 워크아웃별 Apple 강도. 12개월 러닝을 앵커 증분 동기화(`mimo_effort_map_v1.json` + 앵커) — 앵커는 첫 동기화가 실제 데이터를 만든 뒤에만 채택(권한 요청 전 빈 결과로 이력이 가려지는 사고 방지). 관계는 있는데 샘플이 없으면 삭제로 반영. 20초 타임아웃. HealthKit 쓰기 없음(`toShare: []` 유지). iOS 17은 수동 입력만
- **해석**(`EffortResolver`/`EffortIndex`): 내 입력 > Apple 수동 > Apple 추정 > 없음. 1~10 반올림·클램프(비유한값 방어). 중복 `workoutID`(CloudKit 병합)는 최신 입력 우선. 심박으로 강도를 추정하지 않음
- **기준선**(`EffortBaseline`): 최근 8주 같은 워크아웃 유형의 중앙값(3건 이상), 미달이면 전체 러닝 중앙값. 각 단계에서 **수동 입력이 3건 이상이면 수동 입력만으로** 계산하고 부족할 때만 Apple 값을 섞는다(수동은 본인 척도, Apple 추정은 부풀려짐). 미분류 유형은 같은 유형 집계에서 제외. **유형별 표시(입력 카드 한 줄·성장 탭 표)는 같은 유형만 세고 전체 폴백을 쓰지 않으며, 8주에 3건 미만이면 12주로 창을 넓혀 다시 센다**(`typeSummary`/`typeTable`). 쉬운 날 페이스의 이지런 기준값도 같은 수동 우선 규칙. 절대 임계를 쓰지 않는 이유는 Apple 추정이 이지런에도 5~6을 주는 경향
- **규칙**(`EffortRules`, 최대 3문장): A 라벨 vs 몸 — 이지·LSD가 기준선+2 이상(또는 8 이상이면서 기준선 초과) → 주의 "체감 강도", 기준선 없으면 7 이상만(Apple 추정 단독은 침묵). 템포·인터벌·대회가 기준선−2 이하 → "여유 있게 소화". 그 외 기준선 있으면 "체감 N · 평소 M — 의도와 맞았어요" / B 25°C·습도 75% 이상에서 기준선+1 이상이면 "더운/습한 날은 체감이 1~2 높아지는 게 자연스러워요"(기존 날씨 문장 대체), 기준선 이하면 "체감이 평소 수준" / C 이지·LSD + 기준선+1 이상 + 전후반 페이스 ±3%(1km 미만 부분 스플릿 제외, 홀수면 가운데 제외) → 초반 과속·후반 가속 문장(이때 "의도와 맞았어요"는 생략)
- **부하**(`EffortLoad`): 세션 AU = 강도 × 분. **7일 부하는 활동 상세 퍼포먼스 카드**(이 러닝 날짜 기준 7일, `window`/`rollingAcuteChronic`)에서만 보여주고 성장 탭의 별도 상태 카드는 두지 않는다(중복). 창 합·일별·커버리지·평균 강도, 단조도 = 평균/표준편차, 일별·주별·월별 추이는 통합 기록 카드(`RecordSeries`/`RecordBarChart`)의 **거리 막대**가 담당(막대 길이 = 거리, 막대 색 = 그 구간 평균 강도 — 별도 부하 선은 두지 않는다). 최근 7일 대 이전 4×7일(러닝 없는 창은 부하 0으로 유효, 러닝이 있는데 커버리지 50% 미만인 창만 제외, 유효 창 3개 이상 필요) 라벨 <0.8 낮음 · ≤1.3 유지 · ≤1.5 높음 · 초과 크게 높음. 증감은 최근 7일 대 이전 7일. 달력 주 API(`weekly`/`weeks`/`acuteChronic`)는 주 단위인 레이스 플래너(회복 주 판정)에서 그대로 사용. "부상·위험" 표현 금지(Impellizzeri 2020). 회복·테이퍼 주 판정: 이번 주 평균 ≥ 8주 중앙값+1
- **총평 훈련부하 줄**(`RunSummary.loadLine`, 리듬 카드 하단 총평의 4번째 줄): 상태어는 **4주 평균 대비(`rollingAcuteChronic`)로만** 정한다 — 크게 높음 "4주 평균 대비 크게 높음" · 높음 "4주 평균 대비 높음"(둘 다 노랑) · 유지 "4주 평균 수준" · 낮음 "평소보다 가볍게"(초록), 4주 비교가 아직 안 되면 "7일 N AU 기준"(그것도 없으면 "4주 비교 전"). 최근 7일 대 직전 7일 증감(`rollingWeekOverWeek`)은 창 경계에 걸린 고강도 하루가 빠지는 것만으로 하루 사이 +62%→+23%로 뒤집히므로 **상태어에 쓰지 않고 근거 줄의 숫자로만** 적는다("7일 1,573 AU · 이전 7일 1,274 · 최근 7일 +23%"; "이번 주" 표현 없음). **N일 연속은 상태어에만** 붙인다(3일 이상) — 근거 줄에도 넣으면 같은 말이 두 줄에 겹쳐 보인다. 다음 행동 우선순위: 계획 회복/테이퍼 주 → 4주 평균 대비 높음·단조·4일+ 연속 "30~40분 회복 이지런이나 휴식" → 오늘 고강도 "내일은 이지런이나 휴식" → **어제 기준 라벨이 높음이었는데 오늘 유지/낮음이면 "부하가 내려오는 중이에요. 하루 더 편하게 가면 좋아요."**(저장 없는 히스테리시스 — `RunSummaryInput.acuteChronicYesterday` = `EffortLoad.rollingAcuteChronicYesterday`, 같은 러닝 목록을 하루 전 기준으로 다시 계산) → "충분히 회복됐어요. 빌드업이나 템포런…"은 마지막 고강도 2일+ 경과이면서 **낮음이거나(유지 && 최근 7일 증감 < +15%)**, 어제 라벨이 높음이 아닐 때만. 4주 비교가 없으면 "충분히 회복"을 말하지 않는다. 유지인데 회복 판정에 못 미치면 다음 줄을 비우지 않고 **"지금 리듬을 유지하면 좋아요."**(최근 7일 +15% 이상이면 "…부하가 조금 오르는 중이라 고강도는 하루 간격을 두세요." 추가). 높음·낮음·4주 비교 없음은 그대로(낮음은 회복 문장, 높음은 휴식 문장).
- **쉬운 날 기준**(`EffortPaceTrend`): 최근 12개월 이지·LSD 러닝 강도의 **중앙값**을 본인 이지런 컷오프로 삼는다(3건 이상, 미달이면 4로 폴백) — Apple 추정이 이지런에도 5~6을 주므로 고정 2~4로는 표본이 거의 비었다. 컷오프 이하 러닝 중 템포·인터벌·대회는 제외(유형 미상은 허용). 문구는 개인 기준일 때만 "(본인 이지런 기준)"을 붙인다
- **근거**: Foster 2001(sRPE·단조도) · Seiler & Kjerland 2006 · Ely 2007·El Helou 2012(더위) · Coyle & González-Alonso 2001(심박 드리프트) · Coach Parry 마스터스 이지런(초반 과속·환경 부하·후반 가속)
- **하지 않는 것**: HealthKit 쓰기 · 심박 기반 강도 추정 · 공유 카드 강도 표시 · 레벨별 노출 차등 · 걷기·하이킹 강도

### 6.11 러닝 흐름 문장 (`RecordFlowInsight`)

- **역할**: 성장 탭 러닝 흐름 차트 아래 **상태 한 줄 + 방향 한 줄**. 순수 규칙 엔진(화면·HealthKit 의존 없음), 전부 테스트(`RecordFlowInsightTests`)
- **입력**: `RecordSeries.bars`의 버킷 배열 + 기간 + 쉬운 날 컷오프(`easyEffortCutoff()`, 없으면 `EffortPaceTrend.fallbackCutoff`)
- **상태 줄**: "기간 라벨 · N회 · N km · 쉬운 날 N%" (쉬운 날 % = `meanEffort ≤ 컷오프`인 강도 버킷 ÷ 강도 버킷, 강도 기록이 없으면 그 부분 생략. km는 100 미만만 소수 한 자리)
- **방향 줄**: 창을 **앞뒤 절반**(버킷 개수, 홀수면 뒤가 하나 더)으로 나눠 거리 합계 **±10%** · 거리 가중 평균 페이스 **±5초**(빠름 = 상승) · 강도 버킷 평균 **±1**로 방향을 잡고(임계값과 정확히 같으면 유지), 우선순위대로 문장 하나: 같은 노력에 빨라짐 > 거리·강도 동반 상승(쉬운 날 하나 더) > 거리만 상승 > 회복 구간 > 힘든데 느려짐(더위·수면·피로 점검) > 고른 흐름(주 N회 · 평균 N km). 그 외 조합과 **절반에 3회 미만**이면 방향 없이 상태 줄만
- **하지 않는 것**: 판단어·"부상/위험" 표현 · 공유 카드 표시(앱 화면 전용) · AI 문장 생성

---

## 7. 건강·분석 서비스

### 7.1 컨디션 서비스 (`ConditionService`)

- Open-Meteo API: forecast / archive 엔드포인트
- 런 시간대 시간별 샘플링
- 기온·강수·풍속 파싱
- 앱 내 날씨 캐시(ConditionCache)

### 7.2 HRV 회복 (`HRVRecovery`)

- 7일 기준 중앙값 ± SD로 회복 수준 판정
- 출력: `low` / `normal` / `high`
- 수면 HRV 데이터 필요(Apple Watch)

### 7.3 대회 감지 (`RaceDetector`)

- 번들 대회 CSV와 날짜 정확 매칭
- 출발 위치 근접도 하버사인 거리(≤ 5km)
- 지오 정밀도 2차 검증
- SwiftData로 확인된 대회 매치 저장
- "이거 ○○ 대회였나요?" 제안 UI

### 7.4 힐 스팟 감지 (`HillSpotDetector`)

- 내장 JSON 데이터베이스
- 근접도(반경 내) + 고도 상승 임계값 매칭
- 주요 오르막 구간 이름 식별

### 7.5 인사이트 AI 생성기 (`InsightAIGenerator`)

- FoundationModels 프레임워크(iOS 26+, A17 Pro·M1)
- 한국어 인사이트 상세 문장 재작성
- 30자 이내 제약
- 미지원 기기 → 어휘집 폴백

### 7.6 인사이트 캐시 (`InsightCache`)

- Actor 기반 메모리 + 디스크 캐시
- 버전 기반 무효화(스키마 변경 시 자동 퍼지)

### 7.7 주간 인사이트 (`WeeklyInsight`)

14일 훈련 요약:
- 구성(이지/강도 비율), 신체 신호(피로·보폭·경제성·추진·회전수)
- 스트릭 감지
- 10개 패턴 키(p5–p70): AI 입력 사실 + 2개 언어 템플릿

---

## 8. 데이터 모델 요약

### 핵심 모델

```swift
Activity          // 런/걷기/하이킹 + 거리·시간·페이스·HR·케이던스·파워...
ActivityDetail    // GPS 경로·스플릿·HR존·인터벌·러닝폼·수영 지표
WorkoutType       // interval / longRun / easy / tempo / buildUp / lsd / ...
SplitData         // km 별 페이스·HR·케이던스·파워
HRZoneData        // 존 1–5 체류 시간·비율
IntervalSegment   // 워크아웃 단계(준비운동/운동/회복/정리)
UserLevel         // bucket · ageGrade · best5KEquivSec · vdot
```

### 저장소 모델 (SwiftData)

```swift
CachedActivity    // HealthKit 캐시(HR·칼로리·거리·날씨)
OneLinerEntry     // 텍스트 한마디 + 사진/영상 + JSON 레시피(v2/v3/v4)
RaceMatchRecord   // 대회 매칭 확인 기록
Shoe              // 신발 이름·브랜드·추가일·누적 km
WorkoutStory      // 일기·기분·사진·신발 + effortRPE/effortUpdatedAt(운동 강도 1~10, 옵셔널)
```

### 분석 모델

```swift
TrendAnalysis     // 선형회귀: trendDirection(), trendSentiment()
WeeklyInsight     // 14일 훈련 요약 + 10패턴 키
HeroMetric        // 거리·페이스·시간·심박 (가용성 체크 + 포맷)
FadeAnalysis      // 페이드 원인·드롭%·시작 km·근거 플래그
```

### 커스터마이징 모델

```swift
CustomMiniMeStore // 사용자 정의 아바타 이미지 (Documents JPEG)
StampConfig       // 스탬프 템플릿·색상·위치·크기·애니메이션
OneLinerViewModel // 텍스트·영상 클립 레시피·폰트·색상·위치·효과
```

---

## 9. 기술 스택 현황

| 기술 | 용도 | 상태 |
|---|---|---|
| **SwiftUI** | 전체 UI | 사용 중 |
| **HealthKit** | 운동 데이터 읽기 | 사용 중 |
| **SwiftData** | 캐시·설정·OneLiner 저장 | 사용 중 |
| **Swift Charts** | 페이스·심박·고도 차트 | 사용 중 |
| **MapKit** | GPS 경로 지도·스냅샷 | 사용 중 |
| **AVFoundation** | 영상 합성·내보내기 | 사용 중 |
| **ImageRenderer** | 뷰 → 정지 이미지 | 사용 중 |
| **PhotosPicker** | 사진·영상 선택 | 사용 중 |
| **ShareLink** | iOS 공유 시트 | 사용 중 |
| **Open-Meteo API** | 날씨 데이터 백필 | 사용 중 |
| **FoundationModels** | 인사이트 AI 문장 (iOS 26+) | 구현 완료 (폴백 포함) |
| **CloudKit** | 스토리·설정 동기화 | 상태 표시 구현 중 |
| **WeatherKit** | 날씨 보완 | 예정 |
| **ImagePlayground** | 미니미 AI 생성 | 예정(폴백=마스코트) |

### 테스트에서 언어 고정 (`MIMORunningTests/Support/LanguageTrait.swift`)
- `AppLanguage.shared.isEnglish`는 프로세스 전역(UserDefaults 동기화)이라, Swift Testing 스위트가 병렬로 돌 때 한 스위트의 영어 전환이 다른 스위트의 한국어 검사로 새어 들어가 간헐 실패를 냈다.
- 해결: `AppLanguage.override`(`@TaskLocal`, 앱에서는 항상 `nil`)를 두고 `isEnglish` 읽기가 `override ?? 저장값`을 거치게 했다. 쓰기(설정 토글)는 그대로 저장값·UserDefaults만 바꾼다.
- 테스트는 전역을 직접 쓰지 않는다 — 한국어 문자열을 검사하는 스위트는 `@Suite("…", .korean)`, 영어 전용 테스트는 `@Test(.english)`, 한 테스트 안에서 일부만 영어로 볼 때는 `inEnglish { … }`(`inKorean`도 있음). 트레이트는 `TestScoping`으로 본문을 `AppLanguage.$override.withValue(_:)` 안에서 실행한다.
- `.serialized`는 언어 때문에만 붙어 있던 스위트에서 제거했다. `InsightEngineHeatTests`만 `InsightEngine.compute`가 UserDefaults(테마·기온 이력)를 쓰므로 유지.

---

## 구현 완료 기능 체크리스트

### 코어
- [x] HealthKit 권한 요청 + 온보딩
- [x] 활동 리스트 (러닝·걷기·하이킹)
- [x] 2단계 캐시 + 증분 동기화
- [x] 쉬는 날 OneLiner 기록
- [x] 활동 상세 (지도·지표·스플릿·HR존·인터벌·폼)

### 엔진
- [x] 레벨 산출 (Riegel·백분위·에이지그레이딩·VDOT)
- [x] 인사이트 엔진 15테마 우선순위 시스템
- [x] 러닝 분석 엔진 (유형별 세부 분석·페이드 원인)
- [x] 워크아웃 타입 자동 분류
- [x] 차트 데이터 빌더 (7레이어·다운샘플·시인성 팔레트)
- [x] FoundationModels AI 인사이트 (iOS 26+, 폴백 포함)
- [x] 내구성 판단 엔진 (S1 케이던스 붕괴 → 근력·케이던스 큐 제안)
- [x] 운동 후 심박 회복 (HRR1 · 기온 통제 추세 · 상세 회복 차트)
- [x] 훈련 계획 플래너 (튠업 대회 주 · 앞선 대회 회복 블록 · 거리별 주간 목표 · 스냅샷 갱신 규칙)
- [x] 운동 강도(RPE) — Apple effort 읽기(iOS 18) · 10막대 입력 · 개인 기준선 규칙 3종 · 이 러닝 기준 7일 sRPE 부하(퍼포먼스 카드) · 쉬운 날(본인 이지런 강도 이하) 페이스 추이 · 회복 주 문장

### 공유 카드
- [x] 카드 3종 (스탬프·원라이너·애슬레틱)
- [x] 스탬프 카드 (9 템플릿, 5 색상 모드, 3 위치 모드)
- [x] 포토 오버레이 카드
- [x] 요약·스플릿·성장 카드
- [x] 각 카드 3–4가지 출력 템플릿(Story/Slide/Video/Static)
- [x] VideoOverlayCard 단일 컴포넌트 통일

### 영상·슬라이드
- [x] 멀티 클립 에디터 (트림·재정렬·지표 칩)
- [x] 1080×1920 영상 내보내기 (HDR→SDR 톤 매핑)
- [x] Ken Burns 사진 슬라이드 합성
- [x] 차트 리플레이 영상
- [x] 경로 영상

### 분석·건강
- [x] 날씨 통합 (Open-Meteo 백필)
- [x] HRV 회복 수준 판정
- [x] 대회 자동 감지 (번들 CSV + 위치 매칭)
- [x] 힐 스팟 감지
- [x] 주간 인사이트 14일 훈련 요약

### 성장·프로필
- [x] 성장 탭 (러닝 흐름 카드: 일간·주간 거리 막대 + 페이스·부하·심박 추세선 · 히트맵 · PR · 마일스톤)
- [x] 추세 지표 드릴다운 (8개 지표, 1분 회복 포함)
- [x] 나 탭 (통계·신발·뱃지·폼 추세)
- [x] 거리 단위 km/마일 토글

### 미구현·예정
- [ ] WeatherKit 통합 (현재 Open-Meteo로 대체)
- [ ] ImagePlayground 미니미 생성
- [ ] CloudKit 전체 동기화
- [ ] 인터벌 재구성 (workoutPlan 기반)
- [ ] 대회 API 연동 (공공데이터포털)
- [ ] MiniMe 마스코트 세트 디자인
